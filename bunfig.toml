[test]
coverageReporter = ["text"]
coverageSkipTestFiles = true
# Coverage thresholds intentionally not enforced here. The pure layers
# (state.js, orchestrator.js) are at 100%. runtime.js is the IO surface
# and is best validated via system specs in the dummy Rails app.
